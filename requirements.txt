#Vbt should be installed first

#### Appserver
django
redis
celery
joblib
whitenoise

#### trading
ib_async
django-filter
psycopg2-binary
python-telegram-bot
ipykernel #for jupyter notebook to find the env
requests
plotly

#### Only if making machine learning, remove the ordner ml if you don't need it.
#### Deactivated by default, as it uses a huge amount of memory. 
# tensorflow #optional
# keras #optional






